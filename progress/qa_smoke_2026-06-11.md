# QA Smoke — Feature 18 (Fase 3 Hardening) — 2026-06-11

**Entorno**: Docker UP (api :3000, ui :3001, db healthy). Verificación API vía curl + browser Playwright MCP.
**Nota operativa**: el watch de NestJS/Vite NO detecta cambios en volúmenes montados de Windows — fue necesario `docker restart tracker-sales-api tracker-sales-ui` para cargar el código nuevo.
**Credenciales**: admin/Admin123! ya NO funciona (password cambiado); usar `admin_qa/Admin123!`. seller1/Seller123! OK.

## Resultados

| Item | Verificación | Resultado |
|------|--------------|-----------|
| 3.1 AuditInterceptor runtime | Crear deal + PATCH /api/deals/:id/stage → row en audit_logs con old/new values | PASS |
| 3.2 dailyCallsGoal | PATCH /api/settings {dailyCallsGoal:15} → GET lo devuelve → mi-dia devuelve 15 (no 10) | PASS |
| 3.2 UI | SettingsPage muestra "Meta diaria de llamadas" | PASS |
| 3.3 Sin 403s como Seller | seller1 navega /clientes, /coaching, /configuracion → consola con 0 errores (solo 401 esperado de login fallido intencional) | PASS |
| 3.3 CoachingPage Seller | Mínimo diario 30 vía fallback del reporte, sin selector de vendedor, sin request a /settings | PASS |
| 3.3 SettingsPage Seller | Read-only, valores visibles, mensaje "Solo los administradores..." | PASS |
| 3.4 Error feedback (login) | Password incorrecta → banner role=alert "Credenciales incorrectas." | PASS |

## Hallazgos menores (no bloqueantes)

1. **audit_logs old_values = "initial"**: al cambiar stage de un deal recién creado, `old_values` registra `{"stage": "initial"}` en vez de `{"stage": "Prospecto"}` — `stageHistory` no incluye el stage inicial al crear el deal. Candidato a fix junto con feature 25 (win/loss usa stage_history).
2. **[RESUELTO EN SESIÓN] Regresión por gating de useSettings**: el backend permite Seller en GET /settings (vista read-only lo necesita). Con la query disabled, Seller veía defaults del frontend en vez de valores reales (verificado: DB con dailyMinPoints=35, UI mostraba 30). Fix: revert del gating solo en `useSettings.ts` (useSellers conserva el suyo). Re-verificado en browser: Seller ve 35 real, consola limpia. Ver "Fix post-QA" en impl_18-phase3-hardening.md.
3. **admin/Admin123! inválido** — credencial de los docs de testing ya no funciona; el card de login aún la anuncia. Usar admin_qa o resetear password de admin.

## Estado de prueba

- Settings restaurado a dailyCallsGoal=10 tras la prueba.
- Deal de prueba y su audit_log eliminados de la DB.
